name: dart_form_builder
description: "This is a dynamic form builder for flutter"

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  drag_and_drop_lists: ^0.4.1
  json_dynamic_widget: ^7.3.1+4
  provider: ^6.1.2

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
